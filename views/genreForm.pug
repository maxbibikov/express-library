extends layout

block content
  h1=title

  form(method='POST' action='')
    div.form-group
      label(for='name') Genre:
      input#name.form-control(type='text' placeholder='Fantasy, Poetry etc.' name='name' value=(genre ? genre.name : ''))
    button.btn.btn-primary(type='submit') 💾 Submit
  hr

  if errors
    h4.text-danger Error
    ul
      for error in errors
        li.text-danger!=error.msg
